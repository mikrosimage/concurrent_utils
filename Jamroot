use-project /boost : [ modules.peek : BOOST_ROOT ] ;

project
    : requirements
        <library>/boost//thread/<link>shared
        <include>src
    :  usage-requirements 
        <library>/boost//thread/<link>shared
        <include>src
    :  build-dir bin
    ;

alias concurrent ;
