use-project /boost : [ modules.peek : BOOST_ROOT ] ;

project
    : requirements
        <library>/boost//thread/<link>shared
        <include>include
    :  usage-requirements 
        <library>/boost//thread/<link>shared
        <include>include
    :  build-dir bin
    ;

alias concurrent ;
