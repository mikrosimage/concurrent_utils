use-project /boost : [ modules.peek : BOOST_ROOT ] ;

project
    : requirements
        <library>/boost//thread/<link>shared
        <include>.
    :  usage-requirements 
        <library>/boost//thread/<link>shared
        <include>.
    :  build-dir bin
    ;

alias concurrent ;
